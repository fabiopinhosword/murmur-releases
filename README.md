# Murmur — downloads

This repository only hosts the download page, release notes and the update
manifest (`version.json`) that the app checks. Builds are attached to
[Releases](https://github.com/fabiopinhosword/murmur-releases/releases).
